services:
  # ============================================================
  # Nginx 反向代理（唯一對外入口）
  # ============================================================
  nginx:
    image: nginx:alpine
    container_name: csp-nginx
    ports:
      - "${NGINX_PORT:-80}:80"
      # 如需 HTTPS，取消下行註解並掛載憑證
      # - "${NGINX_SSL_PORT:-443}:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      # HTTPS 憑證（選用）
      # - ./certs:/etc/nginx/certs:ro
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    depends_on:
      csp:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - csp_network

  # ============================================================
  # CSP Platform - 管理平台
  # ============================================================
  csp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: csp-backend
    expose:
      - "8000"
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
    env_file:
      - ../.env
    environment:
      TZ: Asia/Taipei
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    networks:
      - csp_network

  # ============================================================
  # 以下為模型服務範例，請依你的實際環境取消註解並修改
  # 所有服務透過 csp_network 內部互通，不需對外暴露 port
  # CSP 的代理層會透過內部網路轉發請求
  # ============================================================

  # --- vLLM (LLM) ---
  # vllm-llm:
  #   image: vllm/vllm-openai:latest
  #   container_name: csp-vllm-llm
  #   expose:
  #     - "8000"
  #   volumes:
  #     - /path/to/models:/models
  #   environment:
  #     - NVIDIA_VISIBLE_DEVICES=0
  #   command: >
  #     --model /models/Meta-Llama-3-70B-Instruct
  #     --tensor-parallel-size 1
  #     --gpu-memory-utilization 0.9
  #     --max-model-len 8192
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
  #   networks:
  #     - csp_network

  # --- vLLM (VLM) ---
  # vllm-vlm:
  #   image: vllm/vllm-openai:latest
  #   container_name: csp-vllm-vlm
  #   expose:
  #     - "8000"
  #   volumes:
  #     - /path/to/models:/models
  #   command: >
  #     --model /models/Qwen2-VL-7B-Instruct
  #     --gpu-memory-utilization 0.9
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
  #   networks:
  #     - csp_network

  # --- Triton (Embedding, e.g. nv-embed-v2) ---
  # triton-embedding:
  #   image: nvcr.io/nvidia/tritonserver:24.01-py3
  #   container_name: csp-triton-embedding
  #   expose:
  #     - "8000"
  #   volumes:
  #     - /path/to/triton-model-repo:/models
  #   command: tritonserver --model-repository=/models
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
  #   networks:
  #     - csp_network

  # --- TensorRT-LLM via Triton ---
  # tensorrt-llm:
  #   image: nvcr.io/nvidia/tritonserver:24.01-trtllm-python-py3
  #   container_name: csp-tensorrt-llm
  #   expose:
  #     - "8000"
  #   volumes:
  #     - /path/to/trt-engines:/engines
  #     - /path/to/triton-trt-repo:/models
  #   command: tritonserver --model-repository=/models
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
  #   networks:
  #     - csp_network

  # ============================================================
  # MLSteam Agent（由 MLSteam 平台部署，通常不需要在此啟動）
  # 只需在 .env 的 AUTO_REGISTER_MODELS 中註冊端點即可
  # ============================================================
  # mlsteam-agent:
  #   image: your-agent-image:latest
  #   container_name: csp-mlsteam-agent
  #   expose:
  #     - "8000"
  #   environment:
  #     - BASE_MODEL_URL=http://vllm-llm:8000
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]
  #   restart: unless-stopped
  #   networks:
  #     - csp_network

networks:
  csp_network:
    driver: bridge
